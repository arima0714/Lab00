#!/bin/sh
#$ -cwd

# 実行環境を指定
#$ -l q_core=1
# 実行時間を指定
#$ -l h_rt=6:00:00
#$ -N serial

# moduleコマンドの初期化
. /etc/profile.d/modules.sh
# 各種環境の読み込み
module load cuda
module load openmpi
module load papi
module load gcc
# 実行
../execBenchmarkExcludeBTSPonW.sh 4 4
